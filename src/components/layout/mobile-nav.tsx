"use client"

// Mobile navigation is now handled directly in the header component
// (logo, search, cart, login icons) and the bottom-nav.tsx fixed bar.
// This component is kept as a no-op for import compatibility.

export function MobileNav() {
  return null
}
